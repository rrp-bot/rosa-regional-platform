apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-alb
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
  annotations:
    k8s-sidecar-target-directory: "Infrastructure"
data:
  alb.json: {{ .Files.Get "dashboards/infrastructure/alb.json" | quote }}
