apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-etcd-health
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
  annotations:
    k8s-sidecar-target-directory: "etcd"
data:
  etcd-health.json: {{ .Files.Get "dashboards/etcd/etcd-health.json" | quote }}
