apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-rds
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
  annotations:
    k8s-sidecar-target-directory: "Infrastructure"
data:
  rds.json: {{ .Files.Get "dashboards/infrastructure/rds.json" | quote }}
